@model IEnumerable<MouseRidersWeb.DTO.PermisoDTO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Permisos)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Permisos)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.Rol }) |
            @Html.ActionLink("Details", "Details", new {  id=item.Rol  }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.Rol })
        </td>
    </tr>
}

</table>
